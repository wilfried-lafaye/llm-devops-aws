apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: air-quality
type: Opaque
stringData:
  # Change these values in production!
  POSTGRES_USER: airquality
  POSTGRES_PASSWORD: airquality_secret_2024
  POSTGRES_DB: airquality
  DATABASE_URL: postgresql://airquality:airquality_secret_2024@postgres:5432/airquality
